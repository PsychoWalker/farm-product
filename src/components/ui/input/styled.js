import styled from "styled-components";

export const InputAddressStyled = styled.input.attrs(() => ({
    placeholder: 'Введите адрес доставки'
})) `
    padding: 14px 131px 13px 12px;
    align-items: center;
    border: 1px solid rgba(0, 0, 0, 0.10);
    background: #F6F6F6;
    margin: 24px 0 20px
`
